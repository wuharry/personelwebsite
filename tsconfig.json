{
  "compilerOptions": {
    /* ===========================
     * 基礎建置資訊
     * =========================== */

    // 增量編譯資訊存放位置，加快重複編譯速度
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",

    /* ===========================
     * 目標輸出環境
     * =========================== */

    // 編譯目標語法版本，ES2022 對應現代瀏覽器
    "target": "ES2022",

    // class field 採用 ECMAScript 標準行為
    "useDefineForClassFields": true,

    // 編譯時可用的內建 API 型別
    "lib": ["ES2022", "DOM", "DOM.Iterable"],

    // 使用原生 ES Module，不轉成 CommonJS
    "module": "ESNext",

    // Vite 環境的型別定義，提供 import.meta.env 等 API 支援
    "types": ["vite/client"],

    // 跳過 node_modules 與內建庫的型別檢查，提升編譯速度
    "skipLibCheck": true,
    "skipDefaultLibCheck": true,

    /* ===========================
     * Bundler 模式（Vite 關鍵設定）
     * =========================== */

    // 告訴 TypeScript：模組解析交給 bundler
    "moduleResolution": "bundler",

    // 允許 import 時寫完整副檔名（符合原生 ESM 規範）
    "allowImportingTsExtensions": true,

    // 允許 import JSON 檔案
    "resolveJsonModule": true,

    // 強制使用明確的 import/export type 語法
    "verbatimModuleSyntax": true,

    // 每個檔案都視為獨立模組，強制加入 export {}
    "moduleDetection": "force",

    // 每個檔案必須是「可獨立轉譯的模組」，Vite/SWC 的前提
    "isolatedModules": true,

    // 不輸出檔案，TS 僅作為型別檢查
    "noEmit": true,

    // 使用 React 19+ 的 JSX runtime
    "jsx": "react-jsx",

    /* ===========================
     * 型別與程式碼品質
     * =========================== */

    // 開啟所有嚴格型別檢查
    "strict": true,

    // 未使用的變數與參數報錯
    "noUnusedLocals": true,
    "noUnusedParameters": true,

    // 限制只能使用可以被抹除的語法，防止 isolatedModules 出錯
    "erasableSyntaxOnly": true,

    // switch case 沒有 break 報錯
    "noFallthroughCasesInSwitch": true,

    // 禁止 import 沒有副作用檢查的模組
    "noUncheckedSideEffectImports": true,

    // 強制檔案名稱大小寫一致（防 Linux 環境爆炸）
    "forceConsistentCasingInFileNames": true,

    // 允許 CommonJS 模組使用預設匯入語法
    "allowSyntheticDefaultImports": true,

    // 自動處理 CommonJS / ESM 互通
    "esModuleInterop": true,

    /* ===========================
     * Path Alias
     * =========================== */

    "paths": {
      "@/*": ["./src/*"]
    }
  },

  "include": ["src"],
  "exclude": ["node_modules", "dist", "build"]
}
